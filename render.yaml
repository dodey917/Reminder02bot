services:
  - type: web
    name: ifart-reminder-bot1  # Change to bot2/bot3 for others
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        value: your_bot_token_here  # Set this in Render dashboard instead for security
